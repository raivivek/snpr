<legend>Pre-given phenotypes</legend>
<p>Here, we prepared a pre-given set of phenotypes so that scientists have less work cleaning the data.</p>
<p>There is no need to answer all, or any, of these phenotypes, as some of these are highly personal.</p>
<p>If you have any questions (or ideas for new phenotypes) please drop us a mail at <%= mail_to "info@opensnp.org"%>!</p>
<%= nested_form_for current_user, :html => { :class => "form-inline", :multipart => true } do |f| %>
    <%= f.fields_for :curated_phenotypes, @curated_phenotypes do |cp| %>
        <% cp.object.each do |cu| # cu is now each curated_phenotype %>
            <div class="well">
                <h4><u><%= cu.name %></u></h4>
                <% if not cu.description.empty? %>
                    <p>Description: <i><%= cu.description %></i></p>
                <% end %>
                <% if cu.is_series # use select_tag and options_for_select %>
                    <% start = cu.curated_phenotype_answers[0].variation_name.to_i %>
                    <% stop = cu.curated_phenotype_answers[1].variation_name.to_i %>
                    <% stepsize = cu.curated_phenotype_answers[2].variation_name.to_i %>
                <% elsif cu.is_multiple # use checkboxes %>
                    <% cu.curated_phenotype_answers.each do |ca| %>
                        <%= label_tag ca.variation_name %>
                    <% end %>
                <% else # use radio_button %>
                    <% cu.curated_phenotype_answers.each do |ca| %>
                        <div class="inline">
                            <%= radio_button_tag cu.id, ca.variation_name %>
                            <%= label_tag ca.variation_name %>
                            </div>
                    <% end %>
                <% end %>
            </div>
        <% end %>
        <%= f.submit "Submit all answers", :class => "btn btn-primary" %>
    <% end %>
<% end %>
